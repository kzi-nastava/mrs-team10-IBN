<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item android:id="@+id/routeFragment"
            android:title="@string/home"
            android:icon="@drawable/ic_home"
            android:orderInCategory="100"/>

        <item android:id="@+id/accountFragment"
            android:title="@string/my_account"
            android:icon="@drawable/ic_account"
            android:orderInCategory="200" />

        <item android:id="@+id/rating"
            android:title="@string/chat_support"
            android:icon="@drawable/ic_chat"
            android:orderInCategory="300"/>

        <item android:id="@+id/ride_history"
            android:title="@string/history_of_rides"
            android:icon="@drawable/ic_map"
            android:orderInCategory="400" />

        <item android:id="@+id/tracking_ride"
            android:title="@string/tracking_ride"
            android:icon="@drawable/ic_car"
            android:orderInCategory="500" />
    </group>
    <group android:id="@+id/logout_group">
        <item android:id="@+id/logout"
            android:title="Logout"
            android:icon="@drawable/ic_logout"
            android:orderInCategory="600" />
    </group>
</menu>